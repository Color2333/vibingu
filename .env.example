# Vibing u - Environment Configuration
# Copy this file to .env and fill in your values

# AI Provider 配置 ("openai" 或 "zhipu")
AI_PROVIDER=zhipu

# 智谱AI 配置 (推荐国内用户使用)
ZHIPU_API_KEY=your-zhipu-api-key-here
ZHIPU_BASE_URL=https://open.bigmodel.cn/api/paas/v4

# OpenAI 配置 (海外用户使用)
OPENAI_API_KEY=sk-your-openai-api-key-here
OPENAI_BASE_URL=https://api.openai.com/v1

# 模型配置 (智谱AI)
# 视觉模型: glm-4.6v (付费高质量) / glm-4.6v-flash (免费)
# 文本模型: glm-4.7 (付费高质量) / glm-4.7-flash (免费)
# 嵌入模型: embedding-3
VISION_MODEL=glm-4.6v
TEXT_MODEL=glm-4.7
SMART_MODEL=glm-4.7
EMBEDDING_MODEL=embedding-3

# Database URL (SQLite for local, can use PostgreSQL for production)
DATABASE_URL=sqlite:///./data/vibingu.db

# 内部 API 地址（用于 Next.js 服务端 rewrites 代理，不暴露到浏览器）
# Docker 部署时: http://backend:8000 | 本地开发时无需设置（自动 fallback 到 localhost:8000）
INTERNAL_API_URL=http://backend:8000

# Authentication (Simple password-based auth)
AUTH_PASSWORD=your-secure-password

# ChromaDB persistence directory
CHROMA_PERSIST_DIR=/app/chroma_db

# Upload directory for images
UPLOAD_DIR=/app/uploads

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://frontend:3000

# Debug mode (set to false in production)
DEBUG=false

# Log level
LOG_LEVEL=INFO
